import React from 'react';


const VideoList = (props) => {
//    const videoList = props.videos.map((video,i) => {
    //   const dob = `${new Date(video.)}`
      return (
         <span>
            {/* <iframe src={video.thumb} alt={video.name}></iframe> */}
            {/* <video controls width="250">
              <source src={} key={i}></source>
            </video>
         */}
         <div>Come back and plug in videos</div>
         </span>
      )
//    })
   
   return( 
   <div>
      <div>
         {videoList}
      </div>
   </div>
   )
}

export default VideoList
